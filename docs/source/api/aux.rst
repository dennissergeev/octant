Parts of octant
===============
Unit of `octant.core.TrackRun` is :py:class:`OctantTrack`, which is a subclass of :py:class:`pandas.DataFrame` with extra methods and properties relevant to analysing cyclone tracking output.

.. autoclass:: octant.core.OctantTrack

Metadata of `octant.core.TrackRun` is stored as a table of settings.

.. autoclass:: octant.parts.TrackSettings
